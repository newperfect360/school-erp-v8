import{useState}from'react';import{readStored,useStoredState,localDate}from'../storage';import{PageHeading}from'../design/SchoolUI';import{notify}from'../components/Feedback';import{queueMessages}from'../services/messageStore';import{renderMessage,defaultWorkflow}from'../services/messageWorkflow';import{exportRows}from'../services/excel';
export default function Fees({onNavigate}){const students=readStored('erp_pro_students',[]),[rows,save]=useStoredState('erp_pro_fee_ledger',[]),[form,setForm]=useState({studentId:'',type:'Tuition',total:'',paid:'',receipt:'',date:localDate()}),[query,setQuery]=useState('');const record=()=>{const total=Number(form.total),paid=Number(form.paid);if(!students.some(s=>s.id===form.studentId)||!form.type.trim()||!form.date||!form.total||!form.paid||!Number.isFinite(total)||!Number.isFinite(paid)||total<0||paid<0||paid>total||!/^\d+(\.\d{1,2})?$/.test(form.total)||!/^\d+(\.\d{1,2})?$/.test(form.paid)||form.date>localDate())return notify('Select student and valid amounts (up to two decimal places); paid cannot exceed total.');if(paid>0&&!form.receipt.trim())return notify('Receipt number required for a payment.');if(form.receipt&&rows.some(r=>r.receipt.toLowerCase()===form.receipt.trim().toLowerCase()))return notify('Receipt number already exists.');const entry={...form,receipt:form.receipt.trim(),total,paid,id:crypto.randomUUID(),createdAt:new Date().toISOString()};if(save([...rows,entry])){try{if(paid>0)queueMessages([{type:'Fee Receipt',studentId:entry.studentId,key:`fee:${entry.id}`,details:{Amount:paid.toFixed(2),Receipt:entry.receipt,Date:entry.date,"Fee Type":entry.type}}])}catch(e){notify(e.message)}setForm({...form,total:'',paid:'',receipt:''});notify('Fee record saved.')}};const visible=rows.filter(r=>[students.find(s=>s.id===r.studentId)?.name,r.receipt,r.type].join(' ').toLowerCase().includes(query.toLowerCase()));return<div className="core-page"><PageHeading title="Fees & Receipts" description="Student-linked fee records. Each row is an independent charge/receipt; previous records are retained. No online payment collection is connected."/><section className="school-panel workflow-panel"><div className="form-grid"><label>Student<select aria-label="Student" value={form.studentId} onChange={e=>setForm({...form,studentId:e.target.value})}><option value="">Choose student</option>{students.map(s=><option key={s.id} value={s.id}>{s.name} · {s.grNo}</option>)}</select></label>{['type','total','paid','receipt','date'].map(k=><label key={k}>{k}<input type={k==='date'?'date':['paid','total'].includes(k)?'number':'text'} step="0.01" value={form[k]} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}</div><button onClick={record}>Save Fee Record</button></section><label>Search fees<input value={query} onChange={e=>setQuery(e.target.value)}/></label><button onClick={()=>exportRows(visible,'fee-ledger.xlsx')}>Export fee ledger</button>{visible.map(r=>{const s=students.find(s=>s.id===r.studentId);return<article className="school-panel workflow-panel" key={r.id}><strong>{s?.name||r.studentId} · {r.type}</strong><p>Paid ₹{r.paid} / ₹{r.total} · Due ₹{r.total-r.paid} · Receipt {r.receipt||'Not applicable'} · {r.date}</p><div className="import-actions">{r.paid>0&&<button disabled={!s} onClick={()=>onNavigate('Communications',{initialStudentIds:[r.studentId],initialMessage:renderMessage('Fee Receipt',s,{Amount:r.paid,Receipt:r.receipt},readStored('erp_pro_message_settings',defaultWorkflow))})}>Prepare payment message</button>}{r.total>r.paid&&<button disabled={!s} onClick={()=>onNavigate('Communications',{initialStudentIds:[r.studentId],initialMessage:renderMessage('Fee Reminder',s,{Amount:r.total-r.paid},readStored('erp_pro_message_settings',defaultWorkflow))})}>Prepare fee reminder</button>}</div></article>})}{!visible.length&&<p>No linked fee records yet.</p>}<details><summary>Previous fee register — retained without guessed student links</summary>{readStored('erp_pro_fees',[]).map(r=><pre key={r.id}>{JSON.stringify(r,null,2)}</pre>)}</details></div>}
