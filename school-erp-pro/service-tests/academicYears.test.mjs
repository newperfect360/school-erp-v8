import test from 'node:test';import assert from 'node:assert/strict';import {normalizeYear,validateYear,tagNewYearRecords,yearForDate} from '../src/services/academicYears.js';
const store=new Map();globalThis.localStorage={getItem:k=>store.get(k)||null};
test('year dates reject overlap and malformed input; year changes preserve existing tags',()=>{const old={id:'2026-27',startDate:'2026-06-01',endDate:'2027-05-31',status:'Open'};store.set('erp_pro_academic_years',JSON.stringify([old]));store.set('erp_pro_academic_context',JSON.stringify({current:'2027-28'}));assert.equal(normalizeYear('2026–27'),'2026-27');assert.equal(normalizeYear('2026-28'),'');assert.throws(()=>validateYear({id:'2027-28',startDate:'2027-05-01',endDate:'2028-05-31'},[old]),/overlap/);assert.equal(yearForDate('2026-09-17'),'2026-27');const before=[{id:'old',academicYear:'2026-27'}];const next=tagNewYearRecords('erp_pro_sports_athletes',[...before,{id:'new'}],before);assert.equal(next[0].academicYear,'2026-27');assert.equal(next[1].academicYear,'2027-28');});
test('closed years reject new records without changing original rows',()=>{store.set('erp_pro_academic_years',JSON.stringify([{id:'2026-27',startDate:'2026-06-01',endDate:'2027-05-31',status:'Closed'}]));assert.throws(()=>tagNewYearRecords('erp_pro_homework',[{id:'new',date:'2026-09-17'}],[]),/closed/);});
