rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Draft only. Never deploy over existing rules without an approved rollout.
    match /{path=**} { allow read, write: if false; }
  }
}
