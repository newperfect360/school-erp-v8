# Perfect Education - Android development build

Current file: `Perfect-Education-v1.0.0-perfectedu-demo.apk`.

App label: Perfect Education. Package: `com.gbsschool.app` (unchanged). This debug APK requires the local development server; it is not a verified production login release. Real production login and physical-device installation remain unverified. See `release.json` for version, size and SHA-256. Historical APK files are retained but are no longer exposed by the current release asset allowlist.
